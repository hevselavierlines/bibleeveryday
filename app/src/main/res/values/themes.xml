<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BibleEveryDay" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_grey</item>
        <item name="colorPrimaryVariant">@color/secondary_grey</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary_grey</item>
        <item name="colorSecondaryVariant">@color/cardview_light_background</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/secondary_grey</item>
        <!-- Customize your theme here. -->
        <item name="android:activatedBackgroundIndicator">@drawable/gridview_activated_background</item>
    </style>

    <style name="Theme.BibleEveryDay.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.BibleEveryDay.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BibleEveryDay.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.BibleEveryDay.ButtonStyle" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:layout_marginLeft">0dip</item>
        <item name="android:layout_marginRight">0dip</item>
        <item name="android:layout_marginTop">0dip</item>
        <item name="android:layout_marginBottom">0dip</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:background">@drawable/keyboard_button</item>
        <item name="android:shape">@drawable/keyboard_button</item>
        <item name="iconTint">@drawable/keyboard_button</item>
        <item name="iconPadding">0dp</item>
        <item name="android:textSize">20dp</item>
    </style>
</resources>